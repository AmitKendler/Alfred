import React, { Component } from 'react';
import { View, ScrollView } from 'react-native';
import { FlightCard, HotelCard, CarCard, RoutesOptionsContainer } from './containers';

const matantest = () => (
    <View>
      <RoutesOptionsContainer />
    </View>
)

export default matantest;
