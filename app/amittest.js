import React, { Component } from 'react';
import { View, ScrollView } from 'react-native';
import { FlightCard, HotelCard, CarCard } from './components';
const amittest = () => (
    <View style={{flex: 1}}>
      <TripChangeWarning/>
      </View>
)

export default amittest;
